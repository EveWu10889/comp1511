#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

int main (void) {
    ch = fgetc(f);
    ch = getchar();
    putchar(ch);
    fgets(line, MAX, stdin);
    fputs(line, stdout);
    FILE *f = fopen(argv[3], "w");
    if (f == NULL) {
        return NULL;
    }
    fscanf(f, "%d\n", &num);
    fprintf(f, "%d\n", i);
    
    fclose(f);
    
    char species[];
    strcmp(species, "hello");
}
